const ApplicationController = require('./application_controller');

class RocksController extends ApplicationController {
  constructor(options) {
    super(options);
  }
}

module.exports = RocksController;
